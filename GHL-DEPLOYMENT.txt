╔══════════════════════════════════════════════════════════════════╗
║           NEXT STEP EQUITY GROUP — GHL DEPLOYMENT               ║
║                                                                  ║
║  3 blocks below. Copy each into the matching GHL field.          ║
║                                                                  ║
║  BLOCK 1 → GHL Custom CSS                                       ║
║  BLOCK 2 → GHL Custom HTML (body content only)                  ║
║  BLOCK 3 → GHL Custom JS                                        ║
║                                                                  ║
║  ALSO ADD to <head> in GHL settings:                             ║
║  <link rel="preconnect" href="https://fonts.googleapis.com">    ║
║  <link rel="preconnect" href="https://fonts.gstatic.com"        ║
║        crossorigin>                                              ║
║  <link href="https://fonts.googleapis.com/css2?family=          ║
║        Inter:wght@400;500;600;700&family=Playfair+Display:      ║
║        wght@600;700;800&display=swap" rel="stylesheet">         ║
╚══════════════════════════════════════════════════════════════════╝

================================================================
  BLOCK 1: COPY INTO GHL CUSTOM CSS
================================================================

/* =============================================
   NEXT STEP EQUITY GROUP — STYLES
   ============================================= */

/* ---------- VARIABLES ---------- */
:root {
  --navy:        #0B1120;
  --navy-light:  #111A2E;
  --navy-mid:    #162036;
  --charcoal:    #1A2332;
  --gold:        #C9A84C;
  --gold-light:  #D4BA6A;
  --gold-dark:   #B8943A;
  --white:       #FFFFFF;
  --white-soft:  #E2E8F0;
  --white-muted: #94A3B8;
  --font-head:   'Playfair Display', Georgia, serif;
  --font-body:   'Inter', system-ui, sans-serif;
  --ease-out:    cubic-bezier(0.16, 1, 0.3, 1);
  --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
}

/* ---------- RESET ---------- */
*, *::before, *::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
  scroll-padding-top: 80px;
}

body {
  font-family: var(--font-body);
  background: var(--navy);
  color: var(--white);
  overflow-x: hidden;
  -webkit-font-smoothing: antialiased;
}

a {
  text-decoration: none;
  color: inherit;
}

button {
  border: none;
  background: none;
  cursor: pointer;
  font-family: inherit;
}

/* ---------- SHARED BUTTONS ---------- */
.btn-gold {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: linear-gradient(135deg, var(--gold), var(--gold-dark));
  color: var(--navy);
  font-weight: 700;
  font-size: 0.95rem;
  padding: 14px 28px;
  border-radius: 50px;
  transition: all 0.3s var(--ease-out);
  box-shadow: 0 4px 20px rgba(201, 168, 76, 0.25);
  letter-spacing: 0.02em;
}

.btn-gold:hover {
  background: linear-gradient(135deg, var(--gold-light), var(--gold));
  box-shadow: 0 6px 30px rgba(201, 168, 76, 0.4);
  transform: translateY(-2px);
}

.btn-outline {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: transparent;
  color: var(--white);
  font-weight: 600;
  font-size: 0.95rem;
  padding: 14px 28px;
  border-radius: 50px;
  border: 2px solid rgba(255, 255, 255, 0.2);
  transition: all 0.3s var(--ease-out);
}

.btn-outline:hover {
  border-color: var(--gold);
  color: var(--gold);
  background: rgba(201, 168, 76, 0.05);
  transform: translateY(-2px);
}

.btn-lg {
  padding: 16px 36px;
  font-size: 1.05rem;
}

/* ---------- PULSE ANIMATION ---------- */
.btn-pulse {
  position: relative;
}

.btn-pulse::after {
  content: '';
  position: absolute;
  inset: -4px;
  border-radius: 50px;
  background: rgba(201, 168, 76, 0.3);
  z-index: -1;
  animation: pulse-ring 2.5s ease-out infinite;
}

@keyframes pulse-ring {
  0%   { transform: scale(1); opacity: 0.6; }
  70%  { transform: scale(1.08); opacity: 0; }
  100% { transform: scale(1.08); opacity: 0; }
}

/* =============================================
   NAVIGATION
   ============================================= */
.navbar {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  z-index: 1000;
  padding: 16px 0;
  transition: all 0.4s var(--ease-out);
}

.navbar.scrolled {
  background: rgba(11, 17, 32, 0.92);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  padding: 10px 0;
  box-shadow: 0 4px 30px rgba(0, 0, 0, 0.3);
  border-bottom: 1px solid rgba(201, 168, 76, 0.1);
}

.nav-container {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 24px;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

/* Logo */
.nav-logo {
  display: flex;
  align-items: center;
  gap: 10px;
  z-index: 10;
}

.logo-icon svg {
  display: block;
}

.logo-text {
  display: flex;
  flex-direction: column;
}

.logo-name {
  font-family: var(--font-head);
  font-weight: 800;
  font-size: 1.35rem;
  color: var(--gold);
  line-height: 1.1;
  letter-spacing: 0.05em;
}

.logo-tagline {
  font-size: 0.6rem;
  color: var(--white-muted);
  letter-spacing: 0.08em;
  text-transform: uppercase;
  font-weight: 500;
}

/* Desktop Nav Links */
.nav-menu {
  display: none;
  align-items: center;
  gap: 32px;
}

.nav-link {
  font-size: 0.875rem;
  font-weight: 500;
  color: var(--white-soft);
  letter-spacing: 0.01em;
  position: relative;
  transition: color 0.3s ease;
}

.nav-link::after {
  content: '';
  position: absolute;
  bottom: -4px;
  left: 0;
  width: 0;
  height: 2px;
  background: var(--gold);
  border-radius: 2px;
  transition: width 0.3s var(--ease-out);
}

.nav-link:hover {
  color: var(--gold);
}

.nav-link:hover::after {
  width: 100%;
}

/* Desktop CTA */
.nav-cta {
  display: none;
  padding: 10px 22px;
  font-size: 0.85rem;
}

/* Hamburger */
.hamburger {
  display: flex;
  flex-direction: column;
  gap: 5px;
  padding: 8px;
  z-index: 10;
}

.hamburger-line {
  display: block;
  width: 24px;
  height: 2px;
  background: var(--white);
  border-radius: 2px;
  transition: all 0.3s var(--ease-out);
}

.hamburger.active .hamburger-line:nth-child(1) {
  transform: translateY(7px) rotate(45deg);
}

.hamburger.active .hamburger-line:nth-child(2) {
  opacity: 0;
  transform: scaleX(0);
}

.hamburger.active .hamburger-line:nth-child(3) {
  transform: translateY(-7px) rotate(-45deg);
}

/* Mobile Menu */
.mobile-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.6);
  z-index: 998;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.4s ease;
}

.mobile-overlay.active {
  opacity: 1;
  pointer-events: auto;
}

.mobile-menu {
  position: fixed;
  top: 0;
  right: 0;
  width: min(340px, 85vw);
  height: 100vh;
  background: var(--navy-light);
  z-index: 999;
  transform: translateX(100%);
  transition: transform 0.4s var(--ease-out);
  display: flex;
  flex-direction: column;
  padding: 24px;
  border-left: 1px solid rgba(201, 168, 76, 0.15);
}

.mobile-menu.active {
  transform: translateX(0);
}

.mobile-menu-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding-bottom: 24px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
}

.mobile-menu-header .logo-name {
  font-size: 1.5rem;
}

.mobile-close {
  font-size: 2rem;
  color: var(--white-muted);
  line-height: 1;
  padding: 4px;
  transition: color 0.3s ease;
}

.mobile-close:hover {
  color: var(--white);
}

.mobile-nav {
  display: flex;
  flex-direction: column;
  padding: 24px 0;
  flex: 1;
}

.mobile-link {
  font-size: 1.1rem;
  font-weight: 500;
  color: var(--white-soft);
  padding: 14px 0;
  border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  transition: all 0.3s ease;
}

.mobile-link:hover {
  color: var(--gold);
  padding-left: 8px;
}

.mobile-cta {
  width: 100%;
  justify-content: center;
  text-align: center;
  margin-bottom: 24px;
}

.mobile-contact {
  display: flex;
  flex-direction: column;
  gap: 12px;
  padding-top: 16px;
  border-top: 1px solid rgba(255, 255, 255, 0.08);
}

.mobile-contact-link {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 0.9rem;
  color: var(--white-muted);
  transition: color 0.3s ease;
}

.mobile-contact-link:hover {
  color: var(--gold);
}

/* =============================================
   HERO SECTION
   ============================================= */
.hero {
  position: relative;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  padding: 120px 24px 0;
  overflow: hidden;
}

.hero-canvas {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
}

.hero-gradient {
  position: absolute;
  inset: 0;
  background:
    radial-gradient(ellipse 80% 60% at 50% 40%, rgba(201, 168, 76, 0.06) 0%, transparent 60%),
    linear-gradient(180deg, rgba(11, 17, 32, 0.3) 0%, var(--navy) 100%);
  z-index: 1;
}

.hero-content {
  position: relative;
  z-index: 2;
  text-align: center;
  max-width: 800px;
  width: 100%;
}

/* Badge */
.hero-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: rgba(201, 168, 76, 0.1);
  border: 1px solid rgba(201, 168, 76, 0.25);
  border-radius: 50px;
  padding: 8px 20px;
  font-size: 0.8rem;
  font-weight: 600;
  color: var(--gold);
  letter-spacing: 0.04em;
  text-transform: uppercase;
  margin-bottom: 28px;
  animation: fadeInUp 0.8s var(--ease-out) 0.2s both;
}

.badge-dot {
  width: 8px;
  height: 8px;
  background: var(--gold);
  border-radius: 50%;
  animation: badge-blink 2s ease-in-out infinite;
}

@keyframes badge-blink {
  0%, 100% { opacity: 1; }
  50%      { opacity: 0.3; }
}

/* Headline */
.hero-headline {
  font-family: var(--font-head);
  font-weight: 800;
  font-size: clamp(2.2rem, 6vw, 4rem);
  line-height: 1.12;
  color: var(--white);
  margin-bottom: 20px;
  animation: fadeInUp 0.8s var(--ease-out) 0.35s both;
}

.hero-headline-accent {
  display: block;
  background: linear-gradient(135deg, var(--gold), var(--gold-light));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* Subheadline */
.hero-subheadline {
  font-size: clamp(1rem, 2.5vw, 1.2rem);
  color: var(--white-muted);
  line-height: 1.7;
  max-width: 600px;
  margin: 0 auto 36px;
  animation: fadeInUp 0.8s var(--ease-out) 0.5s both;
}

/* CTA Group */
.hero-cta-group {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16px;
  margin-bottom: 48px;
  animation: fadeInUp 0.8s var(--ease-out) 0.65s both;
}

/* Stats */
.hero-stats {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 24px;
  animation: fadeInUp 0.8s var(--ease-out) 0.8s both;
}

.hero-stat {
  text-align: center;
}

.hero-stat-number,
.hero-stat-plus,
.hero-stat-decimal {
  font-family: var(--font-head);
  font-weight: 700;
  font-size: clamp(1.6rem, 4vw, 2.2rem);
  color: var(--gold);
  line-height: 1;
}

.hero-stat-label {
  display: block;
  font-size: 0.75rem;
  color: var(--white-muted);
  margin-top: 4px;
  letter-spacing: 0.02em;
}

.hero-stat-divider {
  width: 1px;
  height: 40px;
  background: rgba(255, 255, 255, 0.12);
}

/* =============================================
   TRUST BAR
   ============================================= */
.trust-bar {
  position: relative;
  z-index: 2;
  width: 100%;
  margin-top: auto;
  padding: 24px 0;
  border-top: 1px solid rgba(255, 255, 255, 0.06);
  background: rgba(11, 17, 32, 0.5);
  backdrop-filter: blur(10px);
}

.trust-bar-inner {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 24px;
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  gap: 20px 36px;
}

.trust-item {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 0.85rem;
  color: var(--white-muted);
  font-weight: 500;
  white-space: nowrap;
}

.trust-item svg {
  flex-shrink: 0;
}

/* =============================================
   SHARED SECTION STYLES
   ============================================= */
.section-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 24px;
}

.section-header {
  text-align: center;
  max-width: 680px;
  margin: 0 auto 56px;
}

.section-tag {
  display: inline-block;
  font-size: 0.75rem;
  font-weight: 700;
  color: var(--gold);
  text-transform: uppercase;
  letter-spacing: 0.12em;
  margin-bottom: 16px;
  position: relative;
  padding: 0 20px;
}

.section-tag::before,
.section-tag::after {
  content: '';
  position: absolute;
  top: 50%;
  width: 12px;
  height: 1px;
  background: var(--gold);
}

.section-tag::before { left: 0; }
.section-tag::after  { right: 0; }

.section-title {
  font-family: var(--font-head);
  font-weight: 800;
  font-size: clamp(1.8rem, 4.5vw, 2.8rem);
  line-height: 1.15;
  color: var(--white);
  margin-bottom: 16px;
}

.section-subtitle {
  font-size: clamp(0.95rem, 2vw, 1.1rem);
  color: var(--white-muted);
  line-height: 1.7;
}

/* ---------- Reveal Animation ---------- */
[data-reveal] {
  opacity: 0;
  transform: translateY(32px);
  transition: opacity 0.7s var(--ease-out), transform 0.7s var(--ease-out);
}

[data-reveal].revealed {
  opacity: 1;
  transform: translateY(0);
}

/* =============================================
   PROBLEM SECTION
   ============================================= */
.problems {
  padding: 100px 0;
  background: var(--navy);
  position: relative;
}

.problems::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(201, 168, 76, 0.2), transparent);
}

.problems-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 20px;
}

.problem-card {
  background: linear-gradient(145deg, var(--navy-light), var(--navy-mid));
  border: 1px solid rgba(255, 255, 255, 0.05);
  border-radius: 16px;
  padding: 32px 28px;
  transition: all 0.4s var(--ease-out);
  position: relative;
  overflow: hidden;
}

.problem-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3px;
  background: linear-gradient(90deg, var(--gold), var(--gold-dark));
  transform: scaleX(0);
  transform-origin: left;
  transition: transform 0.4s var(--ease-out);
}

.problem-card:hover {
  border-color: rgba(201, 168, 76, 0.2);
  transform: translateY(-4px);
  box-shadow: 0 12px 40px rgba(0, 0, 0, 0.3), 0 0 60px rgba(201, 168, 76, 0.05);
}

.problem-card:hover::before {
  transform: scaleX(1);
}

.problem-icon {
  width: 56px;
  height: 56px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(201, 168, 76, 0.08);
  border-radius: 14px;
  margin-bottom: 20px;
  border: 1px solid rgba(201, 168, 76, 0.12);
}

.problem-title {
  font-family: var(--font-head);
  font-weight: 700;
  font-size: 1.25rem;
  color: var(--white);
  margin-bottom: 10px;
}

.problem-desc {
  font-size: 0.9rem;
  color: var(--white-muted);
  line-height: 1.65;
}

/* =============================================
   HOW IT WORKS
   ============================================= */
.how-it-works {
  padding: 100px 0;
  background: var(--navy-light);
  position: relative;
}

.how-it-works::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(201, 168, 76, 0.2), transparent);
}

.steps {
  position: relative;
  max-width: 800px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  gap: 48px;
}

/* Connecting Line */
.steps-line {
  display: none;
}

.step {
  display: flex;
  align-items: flex-start;
  gap: 24px;
}

.step-number-wrap {
  flex-shrink: 0;
  width: 52px;
  height: 52px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, var(--gold), var(--gold-dark));
  border-radius: 50%;
  box-shadow: 0 4px 20px rgba(201, 168, 76, 0.3);
  position: relative;
  z-index: 2;
}

.step-number {
  font-family: var(--font-head);
  font-weight: 800;
  font-size: 1.3rem;
  color: var(--navy);
  line-height: 1;
}

.step-content {
  flex: 1;
  padding-top: 4px;
}

.step-icon {
  width: 48px;
  height: 48px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(201, 168, 76, 0.08);
  border: 1px solid rgba(201, 168, 76, 0.12);
  border-radius: 12px;
  margin-bottom: 16px;
}

.step-title {
  font-family: var(--font-head);
  font-weight: 700;
  font-size: 1.35rem;
  color: var(--white);
  margin-bottom: 8px;
}

.step-desc {
  font-size: 0.95rem;
  color: var(--white-muted);
  line-height: 1.7;
  max-width: 480px;
}

.steps-cta {
  text-align: center;
  margin-top: 56px;
}

/* =============================================
   OFFER TYPES
   ============================================= */
.offers {
  padding: 100px 0;
  background: var(--navy);
  position: relative;
}

.offers::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(201, 168, 76, 0.2), transparent);
}

.offers-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 24px;
}

.offer-card {
  background: linear-gradient(145deg, var(--navy-light), var(--navy-mid));
  border: 1px solid rgba(255, 255, 255, 0.06);
  border-radius: 20px;
  padding: 40px 32px;
  position: relative;
  overflow: hidden;
  transition: all 0.4s var(--ease-out);
}

.offer-card:first-child {
  border-color: rgba(201, 168, 76, 0.25);
  box-shadow: 0 0 40px rgba(201, 168, 76, 0.06);
}

.offer-card:hover {
  transform: translateY(-6px);
  box-shadow: 0 20px 50px rgba(0, 0, 0, 0.3);
  border-color: rgba(201, 168, 76, 0.2);
}

.offer-ribbon {
  position: absolute;
  top: 20px;
  right: -32px;
  background: linear-gradient(135deg, var(--gold), var(--gold-dark));
  color: var(--navy);
  font-size: 0.7rem;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  padding: 6px 40px;
  transform: rotate(45deg);
}

.offer-icon-wrap {
  width: 68px;
  height: 68px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(201, 168, 76, 0.08);
  border: 1px solid rgba(201, 168, 76, 0.15);
  border-radius: 18px;
  margin-bottom: 24px;
}

.offer-title {
  font-family: var(--font-head);
  font-weight: 700;
  font-size: 1.5rem;
  color: var(--white);
  margin-bottom: 12px;
}

.offer-desc {
  font-size: 0.95rem;
  color: var(--white-muted);
  line-height: 1.7;
  margin-bottom: 24px;
}

.offer-features {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 12px;
  margin-bottom: 32px;
}

.offer-features li {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 0.9rem;
  color: var(--white-soft);
  font-weight: 500;
}

.offer-features li svg {
  flex-shrink: 0;
}

.offer-btn {
  width: 100%;
  justify-content: center;
  text-align: center;
}

/* =============================================
   REVIEWS SECTION
   ============================================= */
.reviews {
  padding: 100px 0;
  background: var(--navy-light);
  position: relative;
}

.reviews::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(201, 168, 76, 0.2), transparent);
}

.reviews-carousel {
  position: relative;
  overflow: hidden;
}

.reviews-track {
  display: flex;
  gap: 24px;
  transition: transform 0.5s var(--ease-out);
}

.review-card {
  flex: 0 0 100%;
  min-width: 0;
  background: linear-gradient(145deg, var(--navy), var(--navy-mid));
  border: 1px solid rgba(255, 255, 255, 0.06);
  border-radius: 20px;
  padding: 36px 32px;
  display: flex;
  flex-direction: column;
  gap: 20px;
  transition: border-color 0.4s ease;
}

.review-card:hover {
  border-color: rgba(201, 168, 76, 0.2);
}

.review-stars {
  display: flex;
  gap: 3px;
}

.review-text {
  font-size: 1rem;
  color: var(--white-soft);
  line-height: 1.75;
  font-style: italic;
  flex: 1;
  position: relative;
  padding-left: 20px;
}

.review-text::before {
  content: '\201C';
  position: absolute;
  left: 0;
  top: -4px;
  font-family: var(--font-head);
  font-size: 2rem;
  color: var(--gold);
  line-height: 1;
}

.review-author {
  display: flex;
  align-items: center;
  gap: 14px;
  padding-top: 8px;
  border-top: 1px solid rgba(255, 255, 255, 0.06);
}

.review-avatar {
  width: 44px;
  height: 44px;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--gold), var(--gold-dark));
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: var(--font-head);
  font-weight: 800;
  font-size: 1.1rem;
  color: var(--navy);
  flex-shrink: 0;
}

.review-info {
  display: flex;
  flex-direction: column;
}

.review-name {
  font-weight: 700;
  font-size: 0.95rem;
  color: var(--white);
}

.review-location {
  font-size: 0.8rem;
  color: var(--white-muted);
}

/* Carousel Controls */
.reviews-controls {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 20px;
  margin-top: 36px;
}

.reviews-btn {
  width: 44px;
  height: 44px;
  border-radius: 50%;
  border: 1px solid rgba(255, 255, 255, 0.15);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--white-muted);
  transition: all 0.3s ease;
  background: rgba(255, 255, 255, 0.03);
}

.reviews-btn:hover {
  border-color: var(--gold);
  color: var(--gold);
  background: rgba(201, 168, 76, 0.08);
}

.reviews-dots {
  display: flex;
  gap: 8px;
}

.reviews-dot {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.15);
  border: none;
  cursor: pointer;
  transition: all 0.3s ease;
  padding: 0;
}

.reviews-dot.active {
  background: var(--gold);
  transform: scale(1.2);
}

/* =============================================
   CREDIBILITY BAR
   ============================================= */
.credibility {
  padding: 72px 0;
  background: var(--navy);
  position: relative;
  overflow: hidden;
}

.credibility::before,
.credibility::after {
  content: '';
  position: absolute;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(201, 168, 76, 0.2), transparent);
}

.credibility::before { top: 0; }
.credibility::after  { bottom: 0; }

.credibility-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 40px 24px;
  text-align: center;
}

.cred-stat {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
}

.cred-number {
  font-family: var(--font-head);
  font-weight: 800;
  font-size: clamp(2rem, 5vw, 3rem);
  color: var(--gold);
  line-height: 1;
}

.cred-label {
  font-size: 0.85rem;
  color: var(--white-muted);
  font-weight: 500;
  letter-spacing: 0.01em;
}

/* =============================================
   ABOUT SECTION
   ============================================= */
.about {
  padding: 100px 0;
  background: var(--navy-light);
  position: relative;
}

.about::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(201, 168, 76, 0.2), transparent);
}

.about-layout {
  display: flex;
  flex-direction: column;
  gap: 48px;
  align-items: center;
}

/* Photo Column */
.about-photo-col {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20px;
}

.about-photo-frame {
  position: relative;
  width: 260px;
  height: 320px;
  border-radius: 20px;
  overflow: hidden;
}

.about-photo {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  filter: brightness(0.95);
  transition: filter 0.4s ease, transform 0.6s var(--ease-out);
}

.about-photo-frame:hover .about-photo {
  filter: brightness(1);
  transform: scale(1.03);
}

.about-photo-border {
  position: absolute;
  inset: 0;
  border-radius: 20px;
  border: 2px solid rgba(201, 168, 76, 0.4);
  pointer-events: none;
  z-index: 2;
}

.about-photo-accent {
  position: absolute;
  bottom: -8px;
  right: -8px;
  width: 100%;
  height: 100%;
  border-radius: 20px;
  border: 2px solid rgba(201, 168, 76, 0.15);
  z-index: -1;
}

.about-photo-name {
  text-align: center;
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.about-name {
  font-family: var(--font-head);
  font-weight: 700;
  font-size: 1.25rem;
  color: var(--white);
}

.about-role {
  font-size: 0.85rem;
  color: var(--gold);
  font-weight: 600;
  letter-spacing: 0.03em;
}

/* Text Column */
.about-text-col {
  display: flex;
  flex-direction: column;
  gap: 36px;
}

.about-block-title {
  font-family: var(--font-head);
  font-weight: 700;
  font-size: 1.3rem;
  color: var(--gold);
  margin-bottom: 12px;
  position: relative;
  padding-left: 18px;
}

.about-block-title::before {
  content: '';
  position: absolute;
  left: 0;
  top: 4px;
  width: 4px;
  height: calc(100% - 4px);
  background: linear-gradient(180deg, var(--gold), var(--gold-dark));
  border-radius: 4px;
}

.about-block-text {
  font-size: 0.95rem;
  color: var(--white-muted);
  line-height: 1.8;
}

/* Values */
.about-values {
  display: flex;
  flex-direction: column;
  gap: 20px;
  padding-top: 8px;
}

.about-value {
  display: flex;
  align-items: flex-start;
  gap: 16px;
  padding: 20px;
  background: rgba(201, 168, 76, 0.04);
  border: 1px solid rgba(201, 168, 76, 0.1);
  border-radius: 14px;
  transition: all 0.3s ease;
}

.about-value:hover {
  border-color: rgba(201, 168, 76, 0.25);
  background: rgba(201, 168, 76, 0.07);
}

.about-value-icon {
  width: 42px;
  height: 42px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(201, 168, 76, 0.1);
  border-radius: 10px;
  flex-shrink: 0;
}

.about-value-title {
  display: block;
  font-weight: 700;
  font-size: 0.95rem;
  color: var(--white);
  margin-bottom: 3px;
}

.about-value-desc {
  display: block;
  font-size: 0.85rem;
  color: var(--white-muted);
  line-height: 1.5;
}

/* =============================================
   WHERE WE BUY
   ============================================= */
.where-we-buy {
  padding: 100px 0;
  background: var(--navy);
  position: relative;
}

.where-we-buy::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(201, 168, 76, 0.2), transparent);
}

.where-subtitle {
  font-family: var(--font-head);
  font-weight: 700;
  font-size: 1.1rem;
  color: var(--white-soft);
  margin-bottom: 20px;
  text-align: center;
}

.where-counties {
  margin-bottom: 48px;
}

.where-tags {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 10px;
}

.where-tag {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 8px 18px;
  border-radius: 50px;
  font-size: 0.85rem;
  font-weight: 500;
  color: var(--white-muted);
  background: rgba(255, 255, 255, 0.04);
  border: 1px solid rgba(255, 255, 255, 0.08);
  transition: all 0.3s ease;
  white-space: nowrap;
}

.where-tag:hover {
  border-color: rgba(201, 168, 76, 0.3);
  color: var(--gold);
  background: rgba(201, 168, 76, 0.06);
}

.where-tag--county {
  padding: 10px 22px;
  font-size: 0.9rem;
  font-weight: 600;
  color: var(--gold);
  background: rgba(201, 168, 76, 0.08);
  border-color: rgba(201, 168, 76, 0.2);
}

.where-tag--county:hover {
  background: rgba(201, 168, 76, 0.14);
  border-color: rgba(201, 168, 76, 0.4);
  transform: translateY(-2px);
  box-shadow: 0 4px 15px rgba(201, 168, 76, 0.15);
}

.where-cities {
  margin-bottom: 36px;
}

.where-note {
  text-align: center;
  font-size: 0.9rem;
  color: var(--white-muted);
}

.where-note a {
  color: var(--gold);
  font-weight: 600;
  border-bottom: 1px solid rgba(201, 168, 76, 0.3);
  transition: border-color 0.3s ease;
}

.where-note a:hover {
  border-color: var(--gold);
}

/* =============================================
   FAQ
   ============================================= */
.faq {
  padding: 100px 0;
  background: var(--navy-light);
  position: relative;
}

.faq::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(201, 168, 76, 0.2), transparent);
}

.faq-list {
  max-width: 800px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.faq-item {
  background: rgba(255, 255, 255, 0.02);
  border: 1px solid rgba(255, 255, 255, 0.06);
  border-radius: 14px;
  overflow: hidden;
  transition: border-color 0.3s ease;
}

.faq-item.active {
  border-color: rgba(201, 168, 76, 0.2);
}

.faq-question {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 20px 24px;
  text-align: left;
  font-size: 1rem;
  font-weight: 600;
  color: var(--white);
  cursor: pointer;
  transition: color 0.3s ease;
}

.faq-question:hover {
  color: var(--gold);
}

.faq-chevron {
  flex-shrink: 0;
  color: var(--gold);
  transition: transform 0.3s var(--ease-out);
}

.faq-item.active .faq-chevron {
  transform: rotate(180deg);
}

.faq-answer {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.4s var(--ease-out), padding 0.4s var(--ease-out);
}

.faq-item.active .faq-answer {
  max-height: 300px;
}

.faq-answer p {
  padding: 0 24px 20px;
  font-size: 0.95rem;
  color: var(--white-muted);
  line-height: 1.75;
}

/* =============================================
   FINAL CTA / CONTACT
   ============================================= */
.final-cta {
  padding: 100px 0;
  background: var(--navy);
  position: relative;
  overflow: hidden;
}

.final-cta::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(201, 168, 76, 0.2), transparent);
}

.final-cta::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 20%;
  width: 60%;
  height: 400px;
  background: radial-gradient(ellipse at 50% 100%, rgba(201, 168, 76, 0.06) 0%, transparent 70%);
  pointer-events: none;
}

.final-cta-layout {
  display: flex;
  flex-direction: column;
  gap: 48px;
}

.final-cta-text {
  text-align: center;
}

.final-cta-subtitle {
  font-size: clamp(0.95rem, 2vw, 1.1rem);
  color: var(--white-muted);
  line-height: 1.7;
  margin-top: 16px;
  max-width: 520px;
  margin-left: auto;
  margin-right: auto;
}

.final-cta-contact {
  display: flex;
  flex-direction: column;
  gap: 16px;
  margin-top: 36px;
  align-items: center;
}

.final-contact-link {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 16px 24px;
  background: rgba(201, 168, 76, 0.05);
  border: 1px solid rgba(201, 168, 76, 0.12);
  border-radius: 14px;
  transition: all 0.3s ease;
  width: 100%;
  max-width: 340px;
}

.final-contact-link:hover {
  border-color: rgba(201, 168, 76, 0.3);
  background: rgba(201, 168, 76, 0.08);
}

.final-contact-icon {
  width: 44px;
  height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(201, 168, 76, 0.1);
  border-radius: 12px;
  flex-shrink: 0;
}

.final-contact-label {
  display: block;
  font-size: 0.75rem;
  color: var(--white-muted);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  font-weight: 600;
}

.final-contact-value {
  display: block;
  font-size: 1.05rem;
  font-weight: 700;
  color: var(--white);
}

.final-cta-trust {
  display: flex;
  justify-content: center;
  gap: 24px;
  margin-top: 28px;
  flex-wrap: wrap;
}

.final-trust-item {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 0.8rem;
  color: var(--white-muted);
  font-weight: 500;
}

/* Form Card */
.final-cta-form-wrap {
  display: flex;
  justify-content: center;
}

.final-cta-form-card {
  background: linear-gradient(145deg, var(--navy-light), var(--navy-mid));
  border: 1px solid rgba(201, 168, 76, 0.15);
  border-radius: 24px;
  padding: 40px 32px;
  width: 100%;
  max-width: 520px;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
}

.form-card-title {
  font-family: var(--font-head);
  font-weight: 700;
  font-size: 1.5rem;
  color: var(--white);
  text-align: center;
  margin-bottom: 4px;
}

.form-card-subtitle {
  font-size: 0.85rem;
  color: var(--white-muted);
  text-align: center;
  margin-bottom: 28px;
}

.contact-form {
  display: flex;
  flex-direction: column;
  gap: 18px;
}

.form-group {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.form-label {
  font-size: 0.8rem;
  font-weight: 600;
  color: var(--white-soft);
  letter-spacing: 0.02em;
}

.form-input {
  padding: 14px 16px;
  background: rgba(255, 255, 255, 0.04);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 10px;
  font-family: var(--font-body);
  font-size: 0.95rem;
  color: var(--white);
  transition: border-color 0.3s ease, background 0.3s ease;
  outline: none;
}

.form-input::placeholder {
  color: var(--white-muted);
  opacity: 0.6;
}

.form-input:focus {
  border-color: var(--gold);
  background: rgba(201, 168, 76, 0.04);
}

.form-textarea {
  resize: vertical;
  min-height: 80px;
}

.form-submit {
  width: 100%;
  justify-content: center;
  margin-top: 6px;
  font-size: 1.05rem;
  padding: 16px 32px;
  cursor: pointer;
  border: none;
}

/* =============================================
   FOOTER
   ============================================= */
.footer {
  padding: 64px 0 0;
  background: var(--navy-light);
  border-top: 1px solid rgba(201, 168, 76, 0.1);
}

.footer-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 40px;
  padding-bottom: 48px;
}

.footer-brand {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.footer-desc {
  font-size: 0.9rem;
  color: var(--white-muted);
  line-height: 1.7;
  max-width: 320px;
}

.footer-heading {
  font-family: var(--font-head);
  font-weight: 700;
  font-size: 1rem;
  color: var(--gold);
  margin-bottom: 16px;
}

.footer-nav {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.footer-nav a {
  font-size: 0.9rem;
  color: var(--white-muted);
  transition: color 0.3s ease;
}

.footer-nav a:hover {
  color: var(--gold);
}

.footer-contact {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.footer-contact-link {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 0.9rem;
  color: var(--white-muted);
  transition: color 0.3s ease;
}

.footer-contact-link:hover {
  color: var(--gold);
}

.footer-bottom {
  padding: 28px 0;
  border-top: 1px solid rgba(255, 255, 255, 0.06);
  text-align: center;
}

.footer-disclaimer {
  font-size: 0.75rem;
  color: rgba(148, 163, 184, 0.6);
  line-height: 1.7;
  max-width: 700px;
  margin: 0 auto 12px;
}

.footer-copy {
  font-size: 0.8rem;
  color: var(--white-muted);
}

/* =============================================
   ANIMATIONS
   ============================================= */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(24px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* =============================================
   RESPONSIVE — TABLET (768px+)
   ============================================= */
@media (min-width: 768px) {
  .hero-cta-group {
    flex-direction: row;
    justify-content: center;
    gap: 20px;
  }

  .hero-stats {
    gap: 40px;
  }

  .trust-bar-inner {
    gap: 20px 48px;
  }

  .problems-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .offers-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  /* Reviews — show 2 cards */
  .review-card {
    flex: 0 0 calc(50% - 12px);
  }

  /* Credibility — 4 columns */
  .credibility-grid {
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
  }

  /* About — side by side */
  .about-layout {
    flex-direction: row;
    align-items: flex-start;
    gap: 56px;
  }

  .about-photo-col {
    flex-shrink: 0;
  }

  .about-photo-frame {
    width: 280px;
    height: 350px;
  }

  /* Final CTA — side by side */
  .final-cta-layout {
    flex-direction: row;
    align-items: flex-start;
    gap: 56px;
  }

  .final-cta-text {
    flex: 1;
    text-align: left;
  }

  .final-cta-subtitle {
    margin-left: 0;
  }

  .final-cta-contact {
    align-items: flex-start;
  }

  .final-cta-trust {
    justify-content: flex-start;
  }

  .final-cta-form-wrap {
    flex-shrink: 0;
    width: 420px;
  }

  /* Footer — 4 columns */
  .footer-grid {
    grid-template-columns: 1.4fr 1fr 1fr 1fr;
    gap: 32px;
  }
}

/* =============================================
   RESPONSIVE — DESKTOP (1024px+)
   ============================================= */
@media (min-width: 1024px) {
  .nav-menu {
    display: flex;
  }

  .nav-cta {
    display: inline-flex;
  }

  .hamburger {
    display: none;
  }

  .hero {
    padding-top: 140px;
  }

  .hero-stats {
    gap: 56px;
  }

  /* Problems — 3 columns */
  .problems-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }

  /* How It Works — horizontal with connecting line */
  .steps {
    flex-direction: row;
    align-items: flex-start;
    gap: 0;
    max-width: 100%;
  }

  .steps-line {
    display: block;
    position: absolute;
    top: 26px;
    left: 26px;
    right: 26px;
    height: 2px;
    background: rgba(255, 255, 255, 0.08);
    z-index: 1;
  }

  .steps-line-fill {
    width: 0;
    height: 100%;
    background: linear-gradient(90deg, var(--gold), var(--gold-dark));
    border-radius: 2px;
    transition: width 1.5s var(--ease-out);
  }

  .steps-line-fill.animate {
    width: 100%;
  }

  .step {
    flex: 1;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 20px;
  }

  .step-content {
    padding-top: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .step-desc {
    max-width: 300px;
  }

  /* Offers — 3 columns */
  .offers-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 28px;
  }

  .offer-card:first-child {
    transform: scale(1.03);
  }

  .offer-card:first-child:hover {
    transform: scale(1.03) translateY(-6px);
  }

  /* Reviews — show 3 cards, hide carousel controls */
  .review-card {
    flex: 0 0 calc(33.333% - 16px);
  }

  .reviews-controls {
    display: none;
  }

  .reviews-track {
    transform: none !important;
  }

  /* About — larger photo */
  .about-photo-frame {
    width: 320px;
    height: 400px;
  }

  .about-layout {
    gap: 72px;
  }

  .final-cta-form-wrap {
    width: 480px;
  }

  .final-cta-layout {
    gap: 72px;
  }
}


================================================================
  BLOCK 2: COPY INTO GHL CUSTOM HTML (body content only)
================================================================


  <!-- ========== NAVIGATION ========== -->
  <header id="navbar" class="navbar">
    <div class="nav-container">
      <a href="#" class="nav-logo">
        <span class="logo-icon">
          <svg width="36" height="36" viewBox="0 0 36 36" fill="none" xmlns="http://www.w3.org/2000/svg">
            <rect x="4" y="16" width="28" height="16" rx="2" stroke="#C9A84C" stroke-width="2"/>
            <path d="M18 4L2 16H34L18 4Z" stroke="#C9A84C" stroke-width="2" stroke-linejoin="round"/>
            <rect x="14" y="22" width="8" height="10" rx="1" stroke="#C9A84C" stroke-width="2"/>
          </svg>
        </span>
        <span class="logo-text">
          <span class="logo-name">NSEG</span>
          <span class="logo-tagline">Next Step Equity Group</span>
        </span>
      </a>

      <nav class="nav-menu" id="navMenu">
        <a href="#how-it-works" class="nav-link">How It Works</a>
        <a href="#about" class="nav-link">About</a>
        <a href="#reviews" class="nav-link">Reviews</a>
        <a href="#where-we-buy" class="nav-link">Where We Buy</a>
        <a href="#faq" class="nav-link">FAQ</a>
        <a href="#contact" class="nav-link">Contact</a>
      </nav>

      <a href="#contact" class="nav-cta btn-gold btn-pulse">Get My Cash Offer</a>

      <button class="hamburger" id="hamburger" aria-label="Toggle navigation menu" aria-expanded="false">
        <span class="hamburger-line"></span>
        <span class="hamburger-line"></span>
        <span class="hamburger-line"></span>
      </button>
    </div>
  </header>

  <!-- Mobile Menu Overlay -->
  <div class="mobile-overlay" id="mobileOverlay"></div>
  <div class="mobile-menu" id="mobileMenu">
    <div class="mobile-menu-header">
      <span class="logo-name">NSEG</span>
      <button class="mobile-close" id="mobileClose" aria-label="Close menu">&times;</button>
    </div>
    <nav class="mobile-nav">
      <a href="#how-it-works" class="mobile-link">How It Works</a>
      <a href="#about" class="mobile-link">About</a>
      <a href="#reviews" class="mobile-link">Reviews</a>
      <a href="#where-we-buy" class="mobile-link">Where We Buy</a>
      <a href="#faq" class="mobile-link">FAQ</a>
      <a href="#contact" class="mobile-link">Contact</a>
    </nav>
    <a href="#contact" class="mobile-cta btn-gold">Get My Cash Offer</a>
    <div class="mobile-contact">
      <a href="tel:4434549521" class="mobile-contact-link">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2"><path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72c.127.96.361 1.903.7 2.81a2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0122 16.92z"/></svg>
        (443) 454-9521
      </a>
      <a href="mailto:admin@nsegmd.com" class="mobile-contact-link">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="M22 4L12 13 2 4"/></svg>
        admin@nsegmd.com
      </a>
    </div>
  </div>

  <!-- ========== HERO SECTION ========== -->
  <section class="hero" id="hero">
    <canvas id="heroCanvas" class="hero-canvas"></canvas>
    <div class="hero-gradient"></div>

    <div class="hero-content">
      <div class="hero-badge">
        <span class="badge-dot"></span>
        Trusted Maryland Home Buyers
      </div>

      <h1 class="hero-headline">
        Sell Your Maryland Home
        <span class="hero-headline-accent">Fast, Fair & Hassle-Free</span>
      </h1>

      <p class="hero-subheadline">
        No repairs. No agent fees. No waiting. Get a guaranteed cash offer in as little as 24 hours and close on your timeline.
      </p>

      <div class="hero-cta-group">
        <a href="#contact" class="btn-gold btn-lg btn-pulse">
          Get My Cash Offer
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </a>
        <a href="#how-it-works" class="btn-outline btn-lg">
          See How It Works
        </a>
      </div>

      <div class="hero-stats">
        <div class="hero-stat">
          <span class="hero-stat-number" data-target="200">0</span><span class="hero-stat-plus">+</span>
          <span class="hero-stat-label">Homes Purchased</span>
        </div>
        <div class="hero-stat-divider"></div>
        <div class="hero-stat">
          <span class="hero-stat-number" data-target="50">0</span><span class="hero-stat-plus">+</span>
          <span class="hero-stat-label">Years Combined Exp.</span>
        </div>
        <div class="hero-stat-divider"></div>
        <div class="hero-stat">
          <span class="hero-stat-number" data-target="4">0</span><span class="hero-stat-decimal">.9</span>
          <span class="hero-stat-label">Google Rating</span>
        </div>
      </div>
    </div>

    <!-- Trust Bar -->
    <div class="trust-bar">
      <div class="trust-bar-inner">
        <div class="trust-item">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/><path d="M9 12l2 2 4-4" stroke="#C9A84C" stroke-width="2"/></svg>
          <span>Licensed &amp; Insured</span>
        </div>
        <div class="trust-item">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
          <span>Close in 7–14 Days</span>
        </div>
        <div class="trust-item">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2"><rect x="2" y="6" width="20" height="12" rx="2"/><path d="M2 10h20"/></svg>
          <span>Zero Fees or Commissions</span>
        </div>
        <div class="trust-item">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2"><path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
          <span>Any Condition, Any Situation</span>
        </div>
        <div class="trust-item">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2"><path d="M17 21v-2a4 4 0 00-4-4H5a4 4 0 00-4-4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 00-3-3.87"/><path d="M16 3.13a4 4 0 010 7.75"/></svg>
          <span>BBB Accredited</span>
        </div>
      </div>
    </div>
  </section>

  <!-- ========== PROBLEM SECTION ========== -->
  <section class="problems" id="problems">
    <div class="section-container">
      <div class="section-header">
        <span class="section-tag">We Understand</span>
        <h2 class="section-title">We Get It. Life Gets Complicated.</h2>
        <p class="section-subtitle">Whatever your situation, we've helped homeowners just like you find a way forward. No judgment — just solutions.</p>
      </div>

      <div class="problems-grid">

        <div class="problem-card" data-reveal>
          <div class="problem-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="1.5">
              <path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2z"/>
              <path d="M9 22V12h6v10"/>
              <line x1="2" y1="2" x2="22" y2="22" stroke="#ff6b6b" stroke-width="2" stroke-linecap="round"/>
            </svg>
          </div>
          <h3 class="problem-title">Foreclosure</h3>
          <p class="problem-desc">Behind on payments? We can close before the bank takes action, protecting your credit and giving you cash to start fresh.</p>
        </div>

        <div class="problem-card" data-reveal>
          <div class="problem-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="1.5">
              <path d="M14 2H6a2 2 0 00-2 2v16a2 2 0 002 2h12a2 2 0 002-2V8z"/>
              <polyline points="14 2 14 8 20 8"/>
              <line x1="16" y1="13" x2="8" y2="13"/>
              <line x1="16" y1="17" x2="8" y2="17"/>
            </svg>
          </div>
          <h3 class="problem-title">Probate</h3>
          <p class="problem-desc">Inherited a property through probate? We handle the complexity so you can settle the estate quickly and move forward.</p>
        </div>

        <div class="problem-card" data-reveal>
          <div class="problem-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="1.5">
              <circle cx="12" cy="12" r="10"/>
              <line x1="12" y1="8" x2="12" y2="12"/>
              <line x1="12" y1="16" x2="12.01" y2="16"/>
            </svg>
          </div>
          <h3 class="problem-title">Tax Liens</h3>
          <p class="problem-desc">Facing tax liens on your property? We can pay off the debt at closing and put the remaining equity in your pocket.</p>
        </div>

        <div class="problem-card" data-reveal>
          <div class="problem-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="1.5">
              <path d="M20.84 4.61a5.5 5.5 0 00-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 00-7.78 7.78L12 21.23l8.84-8.84a5.5 5.5 0 000-7.78z"/>
              <line x1="6" y1="6" x2="18" y2="18" stroke="#ff6b6b" stroke-width="1.5" stroke-linecap="round"/>
            </svg>
          </div>
          <h3 class="problem-title">Divorce</h3>
          <p class="problem-desc">Need to split assets quickly? We buy the property outright so both parties can get their share and move on.</p>
        </div>

        <div class="problem-card" data-reveal>
          <div class="problem-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="1.5">
              <path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2z"/>
              <polyline points="9 22 9 12 15 12 15 22"/>
              <path d="M15 5l3 0" stroke="#C9A84C" stroke-width="1.5" stroke-linecap="round"/>
              <path d="M16.5 3.5l0 3" stroke="#C9A84C" stroke-width="1.5" stroke-linecap="round"/>
            </svg>
          </div>
          <h3 class="problem-title">Inherited Property</h3>
          <p class="problem-desc">Don't want the burden of an inherited house? We'll buy it as-is — no cleanouts, no repairs, no stress.</p>
        </div>

        <div class="problem-card" data-reveal>
          <div class="problem-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="1.5">
              <rect x="2" y="7" width="20" height="14" rx="2"/>
              <path d="M16 7V5a4 4 0 00-8 0v2"/>
              <circle cx="12" cy="14" r="2"/>
              <line x1="12" y1="16" x2="12" y2="18"/>
            </svg>
          </div>
          <h3 class="problem-title">Tired Landlord</h3>
          <p class="problem-desc">Done dealing with tenants, repairs, and headaches? Sell your rental property fast and reclaim your peace of mind.</p>
        </div>

      </div>
    </div>
  </section>

  <!-- ========== HOW IT WORKS ========== -->
  <section class="how-it-works" id="how-it-works">
    <div class="section-container">
      <div class="section-header">
        <span class="section-tag">Simple Process</span>
        <h2 class="section-title">How It Works</h2>
        <p class="section-subtitle">Three simple steps to your cash offer. No hoops, no hassles — just a straightforward path to selling your home.</p>
      </div>

      <div class="steps">
        <div class="steps-line" aria-hidden="true">
          <div class="steps-line-fill"></div>
        </div>

        <div class="step" data-reveal>
          <div class="step-number-wrap">
            <span class="step-number">1</span>
          </div>
          <div class="step-content">
            <div class="step-icon">
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="1.5">
                <path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72c.127.96.361 1.903.7 2.81a2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0122 16.92z"/>
              </svg>
            </div>
            <h3 class="step-title">Contact Us</h3>
            <p class="step-desc">Fill out our quick form or give us a call. Tell us about your property — any condition, any situation. It takes less than 2 minutes.</p>
          </div>
        </div>

        <div class="step" data-reveal>
          <div class="step-number-wrap">
            <span class="step-number">2</span>
          </div>
          <div class="step-content">
            <div class="step-icon">
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="1.5">
                <path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2z"/>
                <polyline points="9 22 9 12 15 12 15 22"/>
                <circle cx="18" cy="5" r="3" fill="#C9A84C" stroke="none"/>
              </svg>
            </div>
            <h3 class="step-title">Get Your Offer</h3>
            <p class="step-desc">We'll evaluate your property and present a fair, no-obligation cash offer within 24 hours. No pressure, no hidden fees.</p>
          </div>
        </div>

        <div class="step" data-reveal>
          <div class="step-number-wrap">
            <span class="step-number">3</span>
          </div>
          <div class="step-content">
            <div class="step-icon">
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="1.5">
                <rect x="2" y="6" width="20" height="12" rx="2"/>
                <path d="M2 10h20"/>
                <path d="M6 14h4"/>
              </svg>
            </div>
            <h3 class="step-title">Get Paid &amp; Close</h3>
            <p class="step-desc">Accept the offer and choose your closing date — as fast as 7 days. We handle everything. You walk away with cash in hand.</p>
          </div>
        </div>
      </div>

      <div class="steps-cta" data-reveal>
        <a href="#contact" class="btn-gold btn-lg btn-pulse">
          Start My Offer
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </a>
      </div>
    </div>
  </section>

  <!-- ========== OFFER TYPES ========== -->
  <section class="offers" id="offers">
    <div class="section-container">
      <div class="section-header">
        <span class="section-tag">Our Solutions</span>
        <h2 class="section-title">Three Ways We Can Help</h2>
        <p class="section-subtitle">Every homeowner's situation is unique. That's why we offer multiple solutions tailored to your needs.</p>
      </div>

      <div class="offers-grid">

        <div class="offer-card" data-reveal>
          <div class="offer-ribbon">Most Popular</div>
          <div class="offer-icon-wrap">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="1.5">
              <rect x="2" y="6" width="20" height="12" rx="2"/>
              <path d="M2 10h20"/>
              <path d="M6 14h2"/>
              <path d="M12 14h4"/>
            </svg>
          </div>
          <h3 class="offer-title">Cash Offer</h3>
          <p class="offer-desc">Get a competitive all-cash offer with no financing contingencies. Close in as few as 7 days on your schedule.</p>
          <ul class="offer-features">
            <li>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
              Close in 7–14 days
            </li>
            <li>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
              No repairs needed
            </li>
            <li>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
              Zero fees or commissions
            </li>
            <li>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
              Guaranteed close
            </li>
          </ul>
          <a href="#contact" class="btn-gold offer-btn">Get Cash Offer</a>
        </div>

        <div class="offer-card offer-card--alt" data-reveal>
          <div class="offer-icon-wrap">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="1.5">
              <path d="M12 2L2 7l10 5 10-5-10-5z"/>
              <path d="M2 17l10 5 10-5"/>
              <path d="M2 12l10 5 10-5"/>
            </svg>
          </div>
          <h3 class="offer-title">Creative Finance</h3>
          <p class="offer-desc">Want a higher purchase price? Our creative financing options — like subject-to or seller financing — can maximize your payout.</p>
          <ul class="offer-features">
            <li>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
              Higher purchase price
            </li>
            <li>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
              Monthly cash flow option
            </li>
            <li>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
              Flexible terms
            </li>
            <li>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
              Win-win structure
            </li>
          </ul>
          <a href="#contact" class="btn-outline offer-btn">Learn More</a>
        </div>

        <div class="offer-card offer-card--alt" data-reveal>
          <div class="offer-icon-wrap">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="1.5">
              <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
              <path d="M9 12l2 2 4-4"/>
            </svg>
          </div>
          <h3 class="offer-title">Foreclosure Exit</h3>
          <p class="offer-desc">Facing foreclosure? We specialize in stopping the clock. We can close before auction and help you walk away with dignity and cash.</p>
          <ul class="offer-features">
            <li>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
              Stop foreclosure fast
            </li>
            <li>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
              Protect your credit
            </li>
            <li>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
              Close before auction
            </li>
            <li>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
              Cash at closing
            </li>
          </ul>
          <a href="#contact" class="btn-outline offer-btn">Get Help Now</a>
        </div>

      </div>
    </div>
  </section>

  <!-- ========== REVIEWS SECTION ========== -->
  <section class="reviews" id="reviews">
    <div class="section-container">
      <div class="section-header">
        <span class="section-tag">Testimonials</span>
        <h2 class="section-title">What Our Clients Say</h2>
        <p class="section-subtitle">Real stories from real Maryland homeowners who trusted us with one of life's biggest decisions.</p>
      </div>

      <div class="reviews-carousel" id="reviewsCarousel">
        <div class="reviews-track" id="reviewsTrack">

          <div class="review-card" data-reveal>
            <div class="review-stars" aria-label="5 out of 5 stars">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
            </div>
            <p class="review-text">"I was facing foreclosure and felt completely hopeless. Jay and his team stepped in, made me a fair cash offer, and closed in 10 days. They literally saved my family's future."</p>
            <div class="review-author">
              <div class="review-avatar">M</div>
              <div class="review-info">
                <span class="review-name">Marcus T.</span>
                <span class="review-location">Baltimore, MD</span>
              </div>
            </div>
          </div>

          <div class="review-card" data-reveal>
            <div class="review-stars" aria-label="5 out of 5 stars">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
            </div>
            <p class="review-text">"After my mother passed, dealing with the inherited property was overwhelming. NSEG handled everything with compassion and professionalism. They made a difficult time so much easier."</p>
            <div class="review-author">
              <div class="review-avatar">S</div>
              <div class="review-info">
                <span class="review-name">Sandra L.</span>
                <span class="review-location">Silver Spring, MD</span>
              </div>
            </div>
          </div>

          <div class="review-card" data-reveal>
            <div class="review-stars" aria-label="5 out of 5 stars">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
            </div>
            <p class="review-text">"I was a tired landlord with three rental properties draining my time and money. NSEG bought all three in one deal. Fair price, zero hassle, and I got my life back. Highly recommend."</p>
            <div class="review-author">
              <div class="review-avatar">D</div>
              <div class="review-info">
                <span class="review-name">David R.</span>
                <span class="review-location">Columbia, MD</span>
              </div>
            </div>
          </div>

          <div class="review-card" data-reveal>
            <div class="review-stars" aria-label="5 out of 5 stars">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
            </div>
            <p class="review-text">"Going through a divorce is hard enough without worrying about selling a house. Jay gave us a cash offer in 24 hours, and we closed in two weeks. Both sides walked away happy."</p>
            <div class="review-author">
              <div class="review-avatar">K</div>
              <div class="review-info">
                <span class="review-name">Karen &amp; James W.</span>
                <span class="review-location">Annapolis, MD</span>
              </div>
            </div>
          </div>

          <div class="review-card" data-reveal>
            <div class="review-stars" aria-label="5 out of 5 stars">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="#C9A84C" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
            </div>
            <p class="review-text">"Tax liens had been hanging over my head for years. Next Step Equity Group paid them off at closing and I still walked away with money in my pocket. I wish I'd called them sooner."</p>
            <div class="review-author">
              <div class="review-avatar">R</div>
              <div class="review-info">
                <span class="review-name">Robert J.</span>
                <span class="review-location">Waldorf, MD</span>
              </div>
            </div>
          </div>

        </div>

        <div class="reviews-controls">
          <button class="reviews-btn reviews-btn--prev" id="reviewsPrev" aria-label="Previous review">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M19 12H5M12 19l-7-7 7-7"/></svg>
          </button>
          <div class="reviews-dots" id="reviewsDots"></div>
          <button class="reviews-btn reviews-btn--next" id="reviewsNext" aria-label="Next review">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
          </button>
        </div>
      </div>
    </div>
  </section>

  <!-- ========== CREDIBILITY BAR ========== -->
  <section class="credibility" id="credibility">
    <div class="section-container">
      <div class="credibility-grid">

        <div class="cred-stat" data-reveal>
          <span class="cred-number" data-count-target="200" data-count-suffix="+">0</span>
          <span class="cred-label">Homes Purchased</span>
        </div>

        <div class="cred-stat" data-reveal>
          <span class="cred-number" data-count-target="15" data-count-suffix="M+" data-count-prefix="$">0</span>
          <span class="cred-label">In Real Estate Transactions</span>
        </div>

        <div class="cred-stat" data-reveal>
          <span class="cred-number" data-count-target="98" data-count-suffix="%">0</span>
          <span class="cred-label">Client Satisfaction Rate</span>
        </div>

        <div class="cred-stat" data-reveal>
          <span class="cred-number" data-count-target="7" data-count-suffix=" Day Avg">0</span>
          <span class="cred-label">Close-to-Cash Timeline</span>
        </div>

      </div>
    </div>
  </section>

  <!-- ========== ABOUT SECTION ========== -->
  <section class="about" id="about">
    <div class="section-container">
      <div class="section-header">
        <span class="section-tag">Who We Are</span>
        <h2 class="section-title">Built on Trust. Driven by Purpose.</h2>
        <p class="section-subtitle">Next Step Equity Group isn't just a business — it's a mission to help Maryland homeowners find their next chapter.</p>
      </div>

      <div class="about-layout">

        <div class="about-photo-col" data-reveal>
          <div class="about-photo-frame">
            <img src="assets/images/jay-sandoval.jpg" alt="Jay Sandoval — Founder of Next Step Equity Group" class="about-photo" loading="lazy">
            <div class="about-photo-border"></div>
            <div class="about-photo-accent"></div>
          </div>
          <div class="about-photo-name">
            <span class="about-name">Jay Sandoval</span>
            <span class="about-role">Founder &amp; CEO</span>
          </div>
        </div>

        <div class="about-text-col">

          <div class="about-block" data-reveal>
            <h3 class="about-block-title">Our Story</h3>
            <p class="about-block-text">Next Step Equity Group was founded with a simple belief: every homeowner deserves a fair, honest solution — no matter their situation. What started as a mission to help families facing foreclosure has grown into a full-service home buying company serving homeowners across Maryland.</p>
          </div>

          <div class="about-block" data-reveal>
            <h3 class="about-block-title">Jay's Mission</h3>
            <!-- PASTE JAY'S BIO HERE -->
            <p class="about-block-text">Jay Sandoval started Next Step Equity Group because he saw too many homeowners being taken advantage of during their most vulnerable moments. His mission is simple: provide transparent, fair offers and treat every homeowner like family. With deep roots in Maryland's communities, Jay and his team have helped hundreds of families turn their toughest real estate challenges into fresh starts.</p>
          </div>

          <div class="about-values" data-reveal>
            <div class="about-value">
              <div class="about-value-icon">
                <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
              </div>
              <div>
                <span class="about-value-title">Integrity First</span>
                <span class="about-value-desc">Honest offers, no hidden fees, no bait-and-switch.</span>
              </div>
            </div>
            <div class="about-value">
              <div class="about-value-icon">
                <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2"><path d="M17 21v-2a4 4 0 00-4-4H5a4 4 0 00-4-4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 00-3-3.87"/><path d="M16 3.13a4 4 0 010 7.75"/></svg>
              </div>
              <div>
                <span class="about-value-title">People Over Profit</span>
                <span class="about-value-desc">We build relationships, not just transactions.</span>
              </div>
            </div>
            <div class="about-value">
              <div class="about-value-icon">
                <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
              </div>
              <div>
                <span class="about-value-title">Speed &amp; Certainty</span>
                <span class="about-value-desc">Fast closings you can count on, every single time.</span>
              </div>
            </div>
          </div>

        </div>
      </div>
    </div>
  </section>

  <!-- ========== WHERE WE BUY ========== -->
  <section class="where-we-buy" id="where-we-buy">
    <div class="section-container">
      <div class="section-header">
        <span class="section-tag">Service Areas</span>
        <h2 class="section-title">Where We Buy Homes in Maryland</h2>
        <p class="section-subtitle">We buy houses across the entire state of Maryland. If your property is in any of these areas, we'd love to make you an offer.</p>
      </div>

      <div class="where-counties" data-reveal>
        <h3 class="where-subtitle">Featured Counties</h3>
        <div class="where-tags where-tags--counties">
          <span class="where-tag where-tag--county">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/><circle cx="12" cy="10" r="3"/></svg>
            Montgomery County
          </span>
          <span class="where-tag where-tag--county">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/><circle cx="12" cy="10" r="3"/></svg>
            Prince George's County
          </span>
          <span class="where-tag where-tag--county">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/><circle cx="12" cy="10" r="3"/></svg>
            Baltimore County
          </span>
          <span class="where-tag where-tag--county">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/><circle cx="12" cy="10" r="3"/></svg>
            Anne Arundel County
          </span>
          <span class="where-tag where-tag--county">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/><circle cx="12" cy="10" r="3"/></svg>
            Howard County
          </span>
          <span class="where-tag where-tag--county">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/><circle cx="12" cy="10" r="3"/></svg>
            Harford County
          </span>
        </div>
      </div>

      <div class="where-cities" data-reveal>
        <h3 class="where-subtitle">Cities &amp; Communities</h3>
        <div class="where-tags where-tags--cities">
          <span class="where-tag">Aberdeen</span>
          <span class="where-tag">Annapolis</span>
          <span class="where-tag">Baltimore</span>
          <span class="where-tag">Bowie</span>
          <span class="where-tag">Brunswick</span>
          <span class="where-tag">Catonsville</span>
          <span class="where-tag">Columbia</span>
          <span class="where-tag">Cumberland</span>
          <span class="where-tag">Dundalk</span>
          <span class="where-tag">Edgewater</span>
          <span class="where-tag">Edgewood</span>
          <span class="where-tag">Essex</span>
          <span class="where-tag">Frederick</span>
          <span class="where-tag">Gaithersburg</span>
          <span class="where-tag">Germantown</span>
          <span class="where-tag">Glen Burnie</span>
          <span class="where-tag">Hagerstown</span>
          <span class="where-tag">Hughesville</span>
          <span class="where-tag">Indian Head</span>
          <span class="where-tag">La Plata</span>
          <span class="where-tag">Leonardtown</span>
          <span class="where-tag">Lexington Park</span>
          <span class="where-tag">Lusby</span>
          <span class="where-tag">Lutherville-Timonium</span>
          <span class="where-tag">Mechanicsville</span>
          <span class="where-tag">Mount Airy</span>
          <span class="where-tag">Parkville</span>
          <span class="where-tag">Perry Hall</span>
          <span class="where-tag">Potomac</span>
          <span class="where-tag">Rockville</span>
          <span class="where-tag">Salisbury</span>
          <span class="where-tag">Severn</span>
          <span class="where-tag">Silver Spring</span>
          <span class="where-tag">Thurmont</span>
          <span class="where-tag">Towson</span>
          <span class="where-tag">Upper Marlboro</span>
          <span class="where-tag">Waldorf</span>
          <span class="where-tag">Westminster</span>
        </div>
      </div>

      <p class="where-note" data-reveal>Don't see your city? We likely buy there too. <a href="#contact">Contact us</a> to find out.</p>
    </div>
  </section>

  <!-- ========== FAQ ========== -->
  <section class="faq" id="faq">
    <div class="section-container">
      <div class="section-header">
        <span class="section-tag">Common Questions</span>
        <h2 class="section-title">Frequently Asked Questions</h2>
        <p class="section-subtitle">Get answers to the questions we hear most from Maryland homeowners.</p>
      </div>

      <div class="faq-list" id="faqList">

        <div class="faq-item" data-reveal>
          <button class="faq-question" aria-expanded="false">
            <span>How quickly can you close on my house?</span>
            <svg class="faq-chevron" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M6 9l6 6 6-6"/></svg>
          </button>
          <div class="faq-answer">
            <p>We can close in as little as 7 days. However, we work on your timeline — if you need 30 or 60 days, that's perfectly fine too. You choose the closing date that works best for your situation.</p>
          </div>
        </div>

        <div class="faq-item" data-reveal>
          <button class="faq-question" aria-expanded="false">
            <span>Do I need to make repairs before selling?</span>
            <svg class="faq-chevron" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M6 9l6 6 6-6"/></svg>
          </button>
          <div class="faq-answer">
            <p>Absolutely not. We buy houses in any condition — fire damage, mold, foundation issues, hoarding, you name it. We handle all repairs after closing so you don't have to spend a dime.</p>
          </div>
        </div>

        <div class="faq-item" data-reveal>
          <button class="faq-question" aria-expanded="false">
            <span>Are there any fees or commissions?</span>
            <svg class="faq-chevron" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M6 9l6 6 6-6"/></svg>
          </button>
          <div class="faq-answer">
            <p>Zero. There are no agent commissions, no closing costs, and no hidden fees. The cash offer we present is the amount you walk away with. We cover all closing costs.</p>
          </div>
        </div>

        <div class="faq-item" data-reveal>
          <button class="faq-question" aria-expanded="false">
            <span>How do you determine your offer price?</span>
            <svg class="faq-chevron" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M6 9l6 6 6-6"/></svg>
          </button>
          <div class="faq-answer">
            <p>We analyze comparable sales in your area, the condition of the property, current market trends, and the cost of any needed repairs. Our goal is to present a fair offer that reflects true market value while accounting for the speed and convenience we provide.</p>
          </div>
        </div>

        <div class="faq-item" data-reveal>
          <button class="faq-question" aria-expanded="false">
            <span>What if my house is in foreclosure?</span>
            <svg class="faq-chevron" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M6 9l6 6 6-6"/></svg>
          </button>
          <div class="faq-answer">
            <p>We specialize in helping homeowners facing foreclosure. We can often close before the auction date, pay off your mortgage balance, and potentially put cash in your pocket — all while protecting your credit from a full foreclosure on your record.</p>
          </div>
        </div>

        <div class="faq-item" data-reveal>
          <button class="faq-question" aria-expanded="false">
            <span>Is there any obligation if I request an offer?</span>
            <svg class="faq-chevron" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M6 9l6 6 6-6"/></svg>
          </button>
          <div class="faq-answer">
            <p>None at all. Our offers are 100% free with no obligation. If you decide our offer isn't right for you, simply walk away — no pressure, no awkward follow-up calls. We believe in earning your trust, not pressuring you into a decision.</p>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ========== FINAL CTA / CONTACT ========== -->
  <section class="final-cta" id="contact">
    <div class="section-container">
      <div class="final-cta-layout">

        <div class="final-cta-text" data-reveal>
          <span class="section-tag">Ready to Sell?</span>
          <h2 class="section-title">Get Your Free Cash Offer Today</h2>
          <p class="final-cta-subtitle">Fill out the form and a member of our team will reach out within 24 hours with a no-obligation cash offer. Or call us directly.</p>

          <div class="final-cta-contact">
            <a href="tel:4434549521" class="final-contact-link">
              <div class="final-contact-icon">
                <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2"><path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72c.127.96.361 1.903.7 2.81a2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0122 16.92z"/></svg>
              </div>
              <div>
                <span class="final-contact-label">Call Us Directly</span>
                <span class="final-contact-value">(443) 454-9521</span>
              </div>
            </a>
            <a href="mailto:admin@nsegmd.com" class="final-contact-link">
              <div class="final-contact-icon">
                <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="M22 4L12 13 2 4"/></svg>
              </div>
              <div>
                <span class="final-contact-label">Email Us</span>
                <span class="final-contact-value">admin@nsegmd.com</span>
              </div>
            </a>
          </div>

          <div class="final-cta-trust">
            <div class="final-trust-item">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/><path d="M9 12l2 2 4-4"/></svg>
              No obligation
            </div>
            <div class="final-trust-item">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
              Response in 24 hrs
            </div>
            <div class="final-trust-item">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2"><rect x="2" y="6" width="20" height="12" rx="2"/><path d="M2 10h20"/></svg>
              100% free
            </div>
          </div>
        </div>

        <div class="final-cta-form-wrap" data-reveal>
          <div class="final-cta-form-card">
            <h3 class="form-card-title">Get My Cash Offer</h3>
            <p class="form-card-subtitle">Takes less than 60 seconds</p>

            <!-- PASTE GOHIGHLEVEL EMBED HERE -->
            <form class="contact-form" id="contactForm">
              <div class="form-group">
                <label for="formName" class="form-label">Full Name</label>
                <input type="text" id="formName" class="form-input" placeholder="John Doe" required>
              </div>
              <div class="form-group">
                <label for="formPhone" class="form-label">Phone Number</label>
                <input type="tel" id="formPhone" class="form-input" placeholder="(443) 000-0000" required>
              </div>
              <div class="form-group">
                <label for="formEmail" class="form-label">Email Address</label>
                <input type="email" id="formEmail" class="form-input" placeholder="john@example.com" required>
              </div>
              <div class="form-group">
                <label for="formAddress" class="form-label">Property Address</label>
                <input type="text" id="formAddress" class="form-input" placeholder="123 Main St, Baltimore, MD" required>
              </div>
              <div class="form-group">
                <label for="formSituation" class="form-label">Tell Us About Your Situation</label>
                <textarea id="formSituation" class="form-input form-textarea" placeholder="Briefly describe your situation..." rows="3"></textarea>
              </div>
              <button type="submit" class="btn-gold btn-lg form-submit">
                Get My Cash Offer
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
              </button>
            </form>

          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ========== FOOTER ========== -->
  <footer class="footer">
    <div class="section-container">
      <div class="footer-grid">

        <div class="footer-brand">
          <a href="#" class="nav-logo">
            <span class="logo-icon">
              <svg width="32" height="32" viewBox="0 0 36 36" fill="none" xmlns="http://www.w3.org/2000/svg">
                <rect x="4" y="16" width="28" height="16" rx="2" stroke="#C9A84C" stroke-width="2"/>
                <path d="M18 4L2 16H34L18 4Z" stroke="#C9A84C" stroke-width="2" stroke-linejoin="round"/>
                <rect x="14" y="22" width="8" height="10" rx="1" stroke="#C9A84C" stroke-width="2"/>
              </svg>
            </span>
            <span class="logo-text">
              <span class="logo-name">NSEG</span>
              <span class="logo-tagline">Next Step Equity Group</span>
            </span>
          </a>
          <p class="footer-desc">Maryland's trusted home buying company. We help homeowners sell fast for cash — no fees, no repairs, no hassle.</p>
        </div>

        <div class="footer-links-col">
          <h4 class="footer-heading">Quick Links</h4>
          <nav class="footer-nav">
            <a href="#how-it-works">How It Works</a>
            <a href="#about">About Us</a>
            <a href="#reviews">Reviews</a>
            <a href="#where-we-buy">Where We Buy</a>
            <a href="#faq">FAQ</a>
            <a href="#contact">Get an Offer</a>
          </nav>
        </div>

        <div class="footer-links-col">
          <h4 class="footer-heading">Situations</h4>
          <nav class="footer-nav">
            <a href="#problems">Foreclosure</a>
            <a href="#problems">Probate</a>
            <a href="#problems">Tax Liens</a>
            <a href="#problems">Divorce</a>
            <a href="#problems">Inherited Property</a>
            <a href="#problems">Tired Landlord</a>
          </nav>
        </div>

        <div class="footer-links-col">
          <h4 class="footer-heading">Contact</h4>
          <div class="footer-contact">
            <a href="tel:4434549521" class="footer-contact-link">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2"><path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72c.127.96.361 1.903.7 2.81a2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0122 16.92z"/></svg>
              (443) 454-9521
            </a>
            <a href="mailto:admin@nsegmd.com" class="footer-contact-link">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#C9A84C" stroke-width="2"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="M22 4L12 13 2 4"/></svg>
              admin@nsegmd.com
            </a>
          </div>
        </div>

      </div>

      <div class="footer-bottom">
        <p class="footer-disclaimer">Next Step Equity Group is not a licensed real estate brokerage. We are professional home buyers. We buy homes directly from homeowners. Seller is under no obligation to accept any offer. All offers are subject to inspection and verification. Results may vary.</p>
        <p class="footer-copy">&copy; 2025 Next Step Equity Group. All rights reserved.</p>
      </div>
    </div>
  </footer>



================================================================
  BLOCK 3: COPY INTO GHL CUSTOM JS
================================================================

/* =============================================
   NEXT STEP EQUITY GROUP — SCRIPTS
   ============================================= */

(function () {
  'use strict';

  /* ---------- DOM REFS ---------- */
  var navbar      = document.getElementById('navbar');
  var hamburger   = document.getElementById('hamburger');
  var mobileMenu  = document.getElementById('mobileMenu');
  var mobileOverlay = document.getElementById('mobileOverlay');
  var mobileClose = document.getElementById('mobileClose');
  var mobileLinks = document.querySelectorAll('.mobile-link, .mobile-cta');
  var heroCanvas  = document.getElementById('heroCanvas');

  /* ---------- STICKY NAV ON SCROLL ---------- */
  var lastScroll = 0;

  function handleScroll() {
    var scrollY = window.scrollY;
    if (scrollY > 50) {
      navbar.classList.add('scrolled');
    } else {
      navbar.classList.remove('scrolled');
    }
    lastScroll = scrollY;
  }

  window.addEventListener('scroll', handleScroll, { passive: true });

  /* ---------- MOBILE MENU ---------- */
  function openMobileMenu() {
    mobileMenu.classList.add('active');
    mobileOverlay.classList.add('active');
    hamburger.classList.add('active');
    hamburger.setAttribute('aria-expanded', 'true');
    document.body.style.overflow = 'hidden';
  }

  function closeMobileMenu() {
    mobileMenu.classList.remove('active');
    mobileOverlay.classList.remove('active');
    hamburger.classList.remove('active');
    hamburger.setAttribute('aria-expanded', 'false');
    document.body.style.overflow = '';
  }

  hamburger.addEventListener('click', function () {
    var isOpen = mobileMenu.classList.contains('active');
    if (isOpen) {
      closeMobileMenu();
    } else {
      openMobileMenu();
    }
  });

  mobileClose.addEventListener('click', closeMobileMenu);
  mobileOverlay.addEventListener('click', closeMobileMenu);

  mobileLinks.forEach(function (link) {
    link.addEventListener('click', closeMobileMenu);
  });

  /* Close on Escape */
  document.addEventListener('keydown', function (e) {
    if (e.key === 'Escape') closeMobileMenu();
  });

  /* ---------- SMOOTH SCROLL FOR NAV LINKS ---------- */
  document.querySelectorAll('a[href^="#"]').forEach(function (anchor) {
    anchor.addEventListener('click', function (e) {
      var targetId = this.getAttribute('href');
      if (targetId === '#') return;
      var target = document.querySelector(targetId);
      if (target) {
        e.preventDefault();
        target.scrollIntoView({ behavior: 'smooth' });
      }
    });
  });

  /* ---------- ANIMATED STAT COUNTERS ---------- */
  var statNumbers = document.querySelectorAll('.hero-stat-number[data-target]');
  var statsAnimated = false;

  function animateCounters() {
    if (statsAnimated) return;
    statsAnimated = true;

    statNumbers.forEach(function (el) {
      var target = parseInt(el.getAttribute('data-target'), 10);
      var duration = 2000;
      var startTime = null;

      function step(timestamp) {
        if (!startTime) startTime = timestamp;
        var progress = Math.min((timestamp - startTime) / duration, 1);
        /* ease-out cubic */
        var eased = 1 - Math.pow(1 - progress, 3);
        el.textContent = Math.floor(eased * target);
        if (progress < 1) {
          requestAnimationFrame(step);
        } else {
          el.textContent = target;
        }
      }

      requestAnimationFrame(step);
    });
  }

  /* Use Intersection Observer for stat counters */
  var statsSection = document.querySelector('.hero-stats');
  if (statsSection && 'IntersectionObserver' in window) {
    var observer = new IntersectionObserver(function (entries) {
      entries.forEach(function (entry) {
        if (entry.isIntersecting) {
          animateCounters();
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.5 });
    observer.observe(statsSection);
  } else {
    /* Fallback: animate on load */
    animateCounters();
  }

  /* ---------- REVIEWS CAROUSEL ---------- */
  var reviewsTrack = document.getElementById('reviewsTrack');
  var reviewsPrev  = document.getElementById('reviewsPrev');
  var reviewsNext  = document.getElementById('reviewsNext');
  var reviewsDots  = document.getElementById('reviewsDots');

  if (reviewsTrack) {
    var reviewCards = reviewsTrack.querySelectorAll('.review-card');
    var currentSlide = 0;
    var slidesPerView = 1;
    var totalSlides = reviewCards.length;

    function getCardsPerView() {
      if (window.innerWidth >= 1024) return 3;
      if (window.innerWidth >= 768) return 2;
      return 1;
    }

    function getTotalPages() {
      return Math.max(1, totalSlides - slidesPerView + 1);
    }

    function buildDots() {
      reviewsDots.innerHTML = '';
      var pages = getTotalPages();
      for (var i = 0; i < pages; i++) {
        var dot = document.createElement('button');
        dot.className = 'reviews-dot' + (i === 0 ? ' active' : '');
        dot.setAttribute('aria-label', 'Go to review ' + (i + 1));
        dot.dataset.index = i;
        dot.addEventListener('click', function () {
          goToSlide(parseInt(this.dataset.index, 10));
        });
        reviewsDots.appendChild(dot);
      }
    }

    function updateDots() {
      var dots = reviewsDots.querySelectorAll('.reviews-dot');
      dots.forEach(function (dot, i) {
        dot.classList.toggle('active', i === currentSlide);
      });
    }

    function goToSlide(index) {
      var maxSlide = getTotalPages() - 1;
      currentSlide = Math.max(0, Math.min(index, maxSlide));
      var cardWidth = reviewCards[0].offsetWidth + 24;
      reviewsTrack.style.transform = 'translateX(' + -(currentSlide * cardWidth) + 'px)';
      updateDots();
    }

    reviewsPrev.addEventListener('click', function () {
      goToSlide(currentSlide - 1);
    });

    reviewsNext.addEventListener('click', function () {
      goToSlide(currentSlide + 1);
    });

    /* Touch / swipe support */
    var touchStartX = 0;
    var touchEndX = 0;

    reviewsTrack.addEventListener('touchstart', function (e) {
      touchStartX = e.changedTouches[0].screenX;
    }, { passive: true });

    reviewsTrack.addEventListener('touchend', function (e) {
      touchEndX = e.changedTouches[0].screenX;
      var diff = touchStartX - touchEndX;
      if (Math.abs(diff) > 50) {
        if (diff > 0) {
          goToSlide(currentSlide + 1);
        } else {
          goToSlide(currentSlide - 1);
        }
      }
    }, { passive: true });

    function handleResize() {
      var newPerView = getCardsPerView();
      if (newPerView !== slidesPerView) {
        slidesPerView = newPerView;
        buildDots();
        goToSlide(0);
      }
    }

    slidesPerView = getCardsPerView();
    buildDots();
    window.addEventListener('resize', handleResize);
  }

  /* ---------- CREDIBILITY COUNT-UP ---------- */
  var credNumbers = document.querySelectorAll('.cred-number[data-count-target]');

  function animateCredCounters() {
    credNumbers.forEach(function (el) {
      var target = parseInt(el.getAttribute('data-count-target'), 10);
      var suffix = el.getAttribute('data-count-suffix') || '';
      var prefix = el.getAttribute('data-count-prefix') || '';
      var duration = 2200;
      var startTime = null;

      function step(timestamp) {
        if (!startTime) startTime = timestamp;
        var progress = Math.min((timestamp - startTime) / duration, 1);
        var eased = 1 - Math.pow(1 - progress, 3);
        var current = Math.floor(eased * target);
        el.textContent = prefix + current + (progress < 1 ? '' : suffix);
        if (progress < 1) {
          requestAnimationFrame(step);
        } else {
          el.textContent = prefix + target + suffix;
        }
      }

      requestAnimationFrame(step);
    });
  }

  if (credNumbers.length > 0 && 'IntersectionObserver' in window) {
    var credSection = document.querySelector('.credibility');
    var credObserver = new IntersectionObserver(function (entries) {
      entries.forEach(function (entry) {
        if (entry.isIntersecting) {
          animateCredCounters();
          credObserver.unobserve(entry.target);
        }
      });
    }, { threshold: 0.4 });
    credObserver.observe(credSection);
  }

  /* ---------- FAQ ACCORDION ---------- */
  var faqItems = document.querySelectorAll('.faq-item');

  faqItems.forEach(function (item) {
    var question = item.querySelector('.faq-question');
    question.addEventListener('click', function () {
      var isActive = item.classList.contains('active');

      /* Close all */
      faqItems.forEach(function (other) {
        other.classList.remove('active');
        other.querySelector('.faq-question').setAttribute('aria-expanded', 'false');
      });

      /* Open clicked if it wasn't already open */
      if (!isActive) {
        item.classList.add('active');
        question.setAttribute('aria-expanded', 'true');
      }
    });
  });

  /* ---------- SCROLL REVEAL ---------- */
  var revealEls = document.querySelectorAll('[data-reveal]');

  if ('IntersectionObserver' in window) {
    var revealObserver = new IntersectionObserver(function (entries) {
      entries.forEach(function (entry) {
        if (entry.isIntersecting) {
          entry.target.classList.add('revealed');
          revealObserver.unobserve(entry.target);
        }
      });
    }, { threshold: 0.15, rootMargin: '0px 0px -40px 0px' });

    revealEls.forEach(function (el, i) {
      el.style.transitionDelay = (i % 3) * 0.12 + 's';
      revealObserver.observe(el);
    });
  } else {
    revealEls.forEach(function (el) {
      el.classList.add('revealed');
    });
  }

  /* ---------- STEPS CONNECTING LINE ANIMATION ---------- */
  var stepsLineFill = document.querySelector('.steps-line-fill');
  if (stepsLineFill && 'IntersectionObserver' in window) {
    var lineObserver = new IntersectionObserver(function (entries) {
      entries.forEach(function (entry) {
        if (entry.isIntersecting) {
          stepsLineFill.classList.add('animate');
          lineObserver.unobserve(entry.target);
        }
      });
    }, { threshold: 0.3 });
    lineObserver.observe(stepsLineFill.parentElement);
  }

  /* ---------- ANIMATED CANVAS BACKGROUND ---------- */
  if (heroCanvas) {
    var ctx = heroCanvas.getContext('2d');
    var particles = [];
    var particleCount = 60;
    var mouse = { x: -9999, y: -9999 };
    var canvasW, canvasH;

    function resizeCanvas() {
      var rect = heroCanvas.parentElement.getBoundingClientRect();
      canvasW = rect.width;
      canvasH = rect.height;
      heroCanvas.width = canvasW;
      heroCanvas.height = canvasH;
    }

    function Particle() {
      this.x = Math.random() * canvasW;
      this.y = Math.random() * canvasH;
      this.vx = (Math.random() - 0.5) * 0.4;
      this.vy = (Math.random() - 0.5) * 0.4;
      this.radius = Math.random() * 2 + 0.5;
      this.opacity = Math.random() * 0.5 + 0.1;
    }

    function initParticles() {
      particles = [];
      for (var i = 0; i < particleCount; i++) {
        particles.push(new Particle());
      }
    }

    function drawParticles() {
      ctx.clearRect(0, 0, canvasW, canvasH);

      for (var i = 0; i < particles.length; i++) {
        var p = particles[i];

        /* Move */
        p.x += p.vx;
        p.y += p.vy;

        /* Wrap around edges */
        if (p.x < 0) p.x = canvasW;
        if (p.x > canvasW) p.x = 0;
        if (p.y < 0) p.y = canvasH;
        if (p.y > canvasH) p.y = 0;

        /* Draw particle */
        ctx.beginPath();
        ctx.arc(p.x, p.y, p.radius, 0, Math.PI * 2);
        ctx.fillStyle = 'rgba(201, 168, 76, ' + p.opacity + ')';
        ctx.fill();

        /* Connect nearby particles */
        for (var j = i + 1; j < particles.length; j++) {
          var p2 = particles[j];
          var dx = p.x - p2.x;
          var dy = p.y - p2.y;
          var dist = Math.sqrt(dx * dx + dy * dy);

          if (dist < 150) {
            var lineOpacity = (1 - dist / 150) * 0.15;
            ctx.beginPath();
            ctx.moveTo(p.x, p.y);
            ctx.lineTo(p2.x, p2.y);
            ctx.strokeStyle = 'rgba(201, 168, 76, ' + lineOpacity + ')';
            ctx.lineWidth = 0.5;
            ctx.stroke();
          }
        }

        /* Mouse interaction */
        var mdx = p.x - mouse.x;
        var mdy = p.y - mouse.y;
        var mDist = Math.sqrt(mdx * mdx + mdy * mdy);
        if (mDist < 120) {
          var force = (120 - mDist) / 120 * 0.02;
          p.vx += mdx * force;
          p.vy += mdy * force;
        }

        /* Dampen velocity */
        p.vx *= 0.99;
        p.vy *= 0.99;
      }

      requestAnimationFrame(drawParticles);
    }

    resizeCanvas();
    initParticles();
    drawParticles();

    window.addEventListener('resize', function () {
      resizeCanvas();
      if (particles.length === 0) initParticles();
    });

    document.addEventListener('mousemove', function (e) {
      var rect = heroCanvas.getBoundingClientRect();
      mouse.x = e.clientX - rect.left;
      mouse.y = e.clientY - rect.top;
    });
  }

})();
